{% extends 'base.html.twig' %}

{% block title %}Articlee{% endblock %}

{% block body %}
    <h1>Articlee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ articlee.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ articlee.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ articlee.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_art_index') }}">back to list</a>

    <a href="{{ path('app_art_edit', {'id': articlee.id}) }}">edit</a>

    {{ include('art/_delete_form.html.twig') }}
{% endblock %}
